---
all:
  hosts:
    # 👉 Замените <nexushost> на output nexus_vm_external_ip из Terraform
    nexus-01:
      ansible_host: 158.160.60.16
      ansible_user: yc-user
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
  children:
    nexus:
      hosts:
        nexus-01:
  vars:
    ansible_connection_type: paramiko
    ansible_user: yc-user